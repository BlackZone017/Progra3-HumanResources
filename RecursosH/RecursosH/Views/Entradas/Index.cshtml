@model IEnumerable<RecursosH.Models.Entrada>
@using RecursosH.Models

@{
    ViewBag.Title = "Index";
}

<div class="container-mio-2">
    <div class="box-mio-2">

        <h1>Entradas</h1><br />

        @*@using (Html.BeginForm("Index", "Entradas", FormMethod.Get))
            {
                <div class="form-horizontal">
                    <div class="form-group">
                        <div class="col-md-10">
                            @Html.Label("Buscar por Mes") <span></span>


                            @Html.DropDownList("Fecha_Entrada",
                                new SelectList(Enum.GetValues(typeof(Meses))),
                                                    "Seleccione",
                                new { @class = "form-control" })

                            <input type="submit" value="Buscar" class="btn btn-success" /><span></span>
                            <button><a href="./Entradas" class="btn btn-block">Buscar Todos</a></button>
                        </div>
                    </div>
                </div>
            }*@

        <table class="table">
            <tr>
                <th>
                    @Html.DisplayName("Codigo de Empleado")
                </th>
                <th>
                    @Html.DisplayName("Nombre de Empleado")
                </th>
                <th>
                    @Html.DisplayName("Fecha de Entrada")
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.codigoEmpleado)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Empleado)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Fecha_Entrada)
                    </td>
                </tr>
            }

        </table>
        </div>
</div>